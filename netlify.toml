# Static deploy of the Vue frontend. The Python backend (backend/app.py) and
# run.sh are kept for local/offline use only and are not part of this build.
# The router uses hash history (#/wheel), so no SPA redirect rules are needed.
[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"
